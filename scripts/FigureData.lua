-- FigureData
-- PASTE INTO: ServerStorage (as a ModuleScript)
--
-- Shared list of placeholder brainrot figures (name + coin price). Used by
-- both FigurePickups (what spawns on shelves) and ShoppingList (what gets
-- assigned to each shopper) so the two always stay in sync. Swap these for
-- real names/prices later -- nothing else needs to change.

return {
	{ name = "Wobblo Gobblini", price = 2 },
	{ name = "Snarfo Blorpaccio", price = 3 },
	{ name = "Chunga Munga Boom", price = 3 },
	{ name = "Dinglehopper Dan", price = 4 },
	{ name = "Sizzlorino Frizzle", price = 5 },
}
